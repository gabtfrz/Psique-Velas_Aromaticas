// Setup global dos testes — carregado antes de cada suíte (ver vite.config.ts).
// Adiciona os matchers do jest-dom (ex: toBeInTheDocument) ao expect do Vitest.
import '@testing-library/jest-dom/vitest'
